{
  "name": "bank-account-app",
  "version": "1.0.0",
  "description": "Bank Account UI with MuleSoft API + Passkeys (WebAuthn)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo \"No build step required for this app\""
  },
  "engines": {
    "node": "20.x"
  },
  "license": "MIT",
  "dependencies": {
    "@simplewebauthn/server": "^10.0.0",
    "cookie-session": "^2.0.0",
    "express": "^4.18.2"
  }
}
